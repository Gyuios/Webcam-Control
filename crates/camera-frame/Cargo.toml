[package]
name = "camera-frame"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Versioned cross-process frame exchange for Webcam-Control"

[dependencies]
memmap2 = "0.9"

[dev-dependencies]
tempfile = "3"
