[package]
name = "camera-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Versioned IPC contracts for Webcam-Control camera hosts"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
